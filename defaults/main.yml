---
# State of the fail2ban package. Use `present` to make sure the package is
# installed, or use `latest` to make sure the package is always at the latest
# version.
fail2ban_package_state: present

